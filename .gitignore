# Virtual environment (regenerable from requirements.txt)
venv/

# Python caches
__pycache__/
*.pyc

# macOS metadata
.DS_Store

# Embedded clone of an external repo (re-clone with `git clone https://github.com/adobe-research/DeepAFx-ST.git`)
DeepAFx-ST/

# Model checkpoints (large, regenerable from training)
*.pt
*.ckpt
models/

# Tensorboard logs
runs/

# Generated audio outputs (regenerable from scripts + raw data)
outputs/*.wav
outputs/*.flac
outputs/*.aif
outputs/*.aiff
data/raw/
DeepAFx-ST/
outputs/
