# Virtual environment (regenerable from requirements.txt)
venv/

# Python caches
__pycache__/
*.pyc

# macOS metadata
.DS_Store

# Embedded clone of an external repo (re-clone with:
#   git clone https://github.com/adobe-research/DeepAFx-ST.git)
DeepAFx-ST/

# Model checkpoints (large, regenerable from training)
*.pt
*.ckpt
models/

# Tensorboard logs
runs/

# Raw audio dataset and generated outputs (large / regenerable)
data/raw/
outputs/

# Hosting / deployment and scratch folders (not part of the submission)
spaces_deploy/
_to_delete/
