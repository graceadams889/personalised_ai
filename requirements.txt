# Personalised Parametric Mastering Chain — dependencies
#
# Core: everything needed to run the parametric mastering chain — profiling,
# the EQ / compressor / width / limiter stages, validation, and the Gradio UI.
numpy>=1.24.0
scipy>=1.10.0
soundfile>=0.12.0
pyloudnorm>=0.1.0
numba>=0.58.0
pandas>=2.0.0
matplotlib>=3.7.0
gradio>=4.0.0

# Optional — only for the exploratory DeepAFx-ST training branch
# (scripts/01_prepare_data.py … 05_launch_ui.py and the src/ training modules).
# Not required to run the parametric mastering chain; uncomment to use that branch.
# torch>=2.0.0
# torchaudio>=2.0.0
# tqdm>=4.65.0
# pyyaml>=6.0
